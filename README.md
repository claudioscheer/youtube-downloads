# Dependencies

- Python 3;
- `pip install -r requirements.txt`;


# How to run?

- `./youtube-video.sh <URL>` or `./youtube-music.sh <URL>`;


# How to install?

Edit the file [`install.sh`](install.sh) according to your needs.

- `./install.sh`;
- `youtube-video <URL>` or `youtube-music <URL>`;